# Splatica Map

Интерактивная карта для отображения 3D сцен из Splatica. Веб-сервис с красивой картой, на которой отображаются сцены с возможностью просмотра через встроенный viewer.

## Технологии

- **Frontend**: Next.js 14 (App Router), TypeScript, React
- **UI**: Tailwind CSS
- **Карта**: Mapbox GL JS
- **Backend**: Next.js API Routes
- **База данных**: SQLite через Prisma

## Установка и запуск

> **Важно**: Если Node.js не установлен, сначала выполните шаги из [SETUP.md](./SETUP.md)

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_MAPBOX_TOKEN="your_mapbox_token_here"
NEXT_PUBLIC_MAPTILER_KEY="your_maptiler_key_here"
ADMIN_PASSWORD="your_secure_password_here"
```

**Где взять токены:**

**Mapbox Token:**
1. Зарегистрируйтесь на [mapbox.com](https://www.mapbox.com)
2. Перейдите в Account → Access tokens
3. Скопируйте ваш Default Public Token или создайте новый

**MapTiler Key (для кастомного стиля карты):**
1. Зарегистрируйтесь на [maptiler.com](https://www.maptiler.com)
2. Перейдите в Account → API Keys
3. Скопируйте ваш API Key
4. Кастомный стиль использует MapTiler Dark Matter как базу и находится в `public/styles/splatica-dark-neon.json`

### 3. Настройка базы данных

Сгенерируйте Prisma Client и создайте базу данных:

```bash
npx prisma generate
npx prisma db push
```

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 5. Запуск в production

```bash
npm run build
npm start
```

## Использование

### Главная страница

Главная страница (`/`) отображает интерактивную карту со всеми добавленными сценами в стиле Scaniverse Web Map.

### Визуальные особенности

#### Темная карта в стиле Scaniverse

Кастомный темный стиль Mapbox с минималистичным дизайном, максимально похожий на Scaniverse:

- **Почти черный фон** (`#050505`) для максимального контраста со светящимися маркерами
- **Приглушенные дороги** - тонкие линии с низкой непрозрачностью (15%), темно-серый цвет
- **Темная вода** (`#0f0f0f`) для минимального визуального шума
- **Светло-серо-голубые подписи городов** (`#7a9bc4`) вместо белого текста
- **Скрыты POI** (точки интереса) для чистоты карты
- **Тонкие границы** стран и регионов с низкой непрозрачностью

#### Вертикальные светящиеся столбы-маркеры

Уникальный дизайн маркеров в стиле Scaniverse - вертикальные градиентные столбы:

- **Кластеризация маркеров:**
  - При дальнем зуме маркеры автоматически группируются в кластеры
  - Кластеры визуализируются как группа слитых столбов разной высоты (до 5 столбов)
  - Высота столбов в кластере варьируется (50px, 65px, 80px) для визуального разнообразия
  - Каждый столб в кластере имеет свою задержку анимации для эффекта волны
  - Число сцен в кластере отображается на стеклообразном темном фоне с размытием
  - При клике на кластер карта плавно приближается (flyTo) к оптимальному зуму
  - При приближении кластеры красиво распадаются на отдельные маркеры

- **Структура маркера:**
  - Вертикальный столб высотой 60px с градиентом от яркого бирюзово-голубого внизу к прозрачному вверху
  - Шире внизу (8px), уже вверху (2px) - коническая форма
  - Круглое основание (14px) с эффектом свечения
  - Многослойное свечение через `box-shadow` для объемного эффекта

- **Анимации:**
  - Плавная пульсация яркости и непрозрачности (3 секунды)
  - При наведении: увеличение высоты до 70px, усиление свечения
  - При выборе: пульсирующее кольцо под основанием с эффектом расширения

- **Цветовая схема:** бирюзово-голубая (`rgba(100, 200, 255)`) с эффектом свечения

#### Сайдбар и модальное окно

Современный дизайн с эффектами размытия и плавными анимациями:

- **Сайдбар:**
  - Темный фон (`black/80`) с сильным размытием заднего плана (`backdrop-blur-2xl`)
  - Плавная анимация выезда (500ms) с эффектом ease-out
  - Скругленные углы и тонкая граница
  - На мобильных устройствах превращается в bottom sheet (70vh высоты)

- **Модальное окно просмотра:**
  - Затемненный фон (`black/90`) с размытием
  - Белая рамка вокруг iframe с тенью
  - Плавная анимация появления с масштабированием
  - Градиентные кнопки в стиле маркеров (cyan-blue)

#### Контролы карты

Круглые кнопки управления с эффектами:

- **Zoom in/out** - увеличение и уменьшение масштаба
- **Reset view** - плавная анимация возврата к начальному виду (flyTo)
- Стиль: темный фон с размытием, тонкая граница, эффект hover с увеличением

#### Логотип

Блок в левом верхнем углу:
- "Splatica World Map" - основной заголовок
- "Scenes rendered by Splatica" - подзаголовок
- Темный фон с размытием для читаемости

### Взаимодействие

- **Клик по маркеру** - открывает модальное окно с встроенным Splatica Viewer
- **На десктопе** - карта занимает почти весь экран с минимальными отступами
- **На мобильных** - сайдбар превращается в bottom sheet, карта адаптируется под размер экрана

### Админ-панель

Доступ к админ-панели: `/admin`

**Вход:**
1. Перейдите на `/login`
2. Введите пароль из переменной окружения `ADMIN_PASSWORD`

**Управление сценами:**
- **Список сцен** (`/admin`): просмотр всех сцен, редактирование и удаление
- **Добавление сцены** (`/admin/new`): форма для создания новой сцены
- **Редактирование** (`/admin/[id]/edit`): редактирование существующей сцены

**Поля сцены:**
- **Title** (обязательно): название сцены
- **Description** (опционально): описание
- **Splatica URL** (обязательно): ссылка вида `https://app.splatica.com/viewer/2424b5ce`
- **Thumbnail URL** (опционально): ссылка на превью изображение или кнопка "📸 Generate" для автоматического создания скриншота
- **Coordinates** (обязательно): широта и долгота
  - Можно ввести вручную
  - Можно выбрать на карте (кнопка "Pick on Map")
  - Можно вставить Google Maps URL или адрес (кнопка "📍 Parse")

## Структура проекта

```
├── app/
│   ├── api/              # API endpoints
│   │   ├── scenes/       # CRUD для сцен
│   │   └── auth/         # Авторизация
│   ├── admin/            # Админ-панель
│   │   ├── login/        # Страница входа
│   │   ├── new/          # Создание сцены
│   │   └── [id]/edit/    # Редактирование сцены
│   ├── page.tsx          # Главная страница с картой
│   └── layout.tsx        # Корневой layout
├── components/           # React компоненты
│   ├── Map.tsx           # Компонент карты
│   ├── SceneSidebar.tsx  # Боковая панель сцены
│   ├── SceneViewerModal.tsx # Модальное окно viewer
│   └── MapPicker.tsx     # Выбор координат на карте
├── hooks/                # React hooks
│   └── useScenes.ts      # Загрузка сцен
├── lib/                  # Утилиты
│   ├── prisma.ts         # Prisma client
│   ├── auth.ts           # Функции авторизации
│   └── scene-utils.ts    # Утилиты для работы со сценами
├── prisma/
│   └── schema.prisma     # Схема базы данных
└── types/
    └── scene.ts          # TypeScript типы
```

## API Endpoints

### Сцены

- `GET /api/scenes` - получить все сцены
- `GET /api/scenes/[id]` - получить одну сцену
- `POST /api/scenes` - создать сцену (требует авторизации)
- `PUT /api/scenes/[id]` - обновить сцену (требует авторизации)
- `DELETE /api/scenes/[id]` - удалить сцену (требует авторизации)

### Авторизация

- `POST /api/auth/login` - войти (требует `password` в body)
- `POST /api/auth/logout` - выйти
- `GET /api/auth/check` - проверить статус авторизации

## База данных

Модель `Scene`:
- `id` - уникальный идентификатор
- `title` - название
- `description` - описание (опционально)
- `splaticaUrl` - полный URL сцены
- `sceneSlug` - извлеченный slug из URL
- `lat` - широта
- `lng` - долгота
- `thumbnailUrl` - URL превью (опционально)
- `createdAt` - дата создания
- `updatedAt` - дата обновления

### Работа с базой данных

**Просмотр данных:**
```bash
npx prisma studio
```

**Сброс базы данных:**
```bash
rm prisma/dev.db
npx prisma db push
```

## Разработка

### Добавление новых функций

1. Компоненты карты находятся в `components/Map.tsx`
2. API endpoints в `app/api/`
3. Стили настраиваются через Tailwind CSS в `tailwind.config.ts`

### Отладка

- Логи сервера отображаются в консоли терминала
- Логи клиента в консоли браузера
- Prisma Studio для просмотра данных: `npm run db:studio`

## Безопасность

- Пароль администратора хранится в переменных окружения
- Сессия администратора хранится в HTTP-only cookie
- API endpoints для изменения данных защищены проверкой авторизации
- В production используйте `Secure` флаг для cookies (настроено автоматически)

## Кастомный стиль карты

Проект использует кастомный стиль `splatica-dark-neon.json`, который базируется на MapTiler Dark Matter и включает:

- **Тёмная минималистичная база**: почти чёрный фон (#020617)
- **Неоновая sci-fi эстетика**: сине-голубые акценты с эффектом свечения
- **Мягкий glow**: подсветка дорог и границ
- **Лёгкий 3D эффект**: здания с fill-extrusion (отображаются на zoom >= 14)

### Изменение MapTiler ключа

1. Откройте `public/styles/splatica-dark-neon.json`
2. Найдите все вхождения `MAPTILER_KEY_PLACEHOLDER`
3. Замените на ваш MapTiler API Key
4. Или установите переменную окружения `NEXT_PUBLIC_MAPTILER_KEY` (ключ будет подставлен автоматически)

### Неоновые маркеры

Точки на карте отображаются в виде неоновых вертикальных столбиков:

- **Структура**: базовый круг + вертикальный столб с градиентом
- **Эффекты**: пульсация, свечение при наведении, кольцо при выборе
- **Стили**: определены в `app/globals.css` (классы `.scene-marker`, `.marker-pillar`, `.marker-base`, `.marker-ring`)

### Добавление новой точки

Точки автоматически загружаются из базы данных через API `/api/scenes`. Для добавления новой точки используйте админ-панель (`/admin/new`).

## Лицензия

MIT

