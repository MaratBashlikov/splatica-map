# Быстрый старт

## ⚠️ Требования

Перед началом убедитесь, что установлен **Node.js** (версия 18 или выше).

Проверка:
```bash
node --version
npm --version
```

Если команды не найдены, см. [SETUP.md](./SETUP.md) для инструкций по установке Node.js.

## 🚀 Запуск за 5 шагов

### 1. Установите зависимости
```bash
npm install
```

### 2. Настройте переменные окружения

Откройте `.env.local` и заполните:

- **NEXT_PUBLIC_MAPBOX_TOKEN** - получите на https://account.mapbox.com/access-tokens/
- **ADMIN_PASSWORD** - придумайте пароль для админ-панели

### 3. Настройте базу данных
```bash
npx prisma generate
npx prisma db push
```

### 4. Запустите проект
```bash
npm run dev
```

### 5. Откройте в браузере
- Главная страница: http://localhost:3000
- Админ-панель: http://localhost:3000/admin/login

## 📝 Первые шаги

1. Войдите в админ-панель с паролем из `.env.local`
2. Добавьте первую сцену:
   - Нажмите "Add Scene"
   - Заполните форму (обязательно: Title, Splatica URL, координаты)
   - Используйте кнопку "Pick on Map" для выбора координат
3. Вернитесь на главную страницу и увидите сцену на карте!

## ❓ Проблемы?

См. раздел "Решение проблем" в [SETUP.md](./SETUP.md)

